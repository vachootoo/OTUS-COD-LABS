# Построение Underlay сети (OSPF)

## 1. План работы

В качестве Underlay используется **OSPF**.

Основные параметры:

- 2 × Spine
- 3 × Leaf
- все Spine ↔ Leaf соединения работают как L3
- используется `OSPF Area 0`
- `Loopback0` используется как Router-ID
- физические интерфейсы работают как `point-to-point`
- для P2P используются сети `/31`

### Основные этапы

1. Настроить `Loopback0`.
2. Настроить IP-адреса на физических интерфейсах.
3. Включить маршрутизацию:

```text
ip routing
```

4. Настроить OSPF:

```text
router ospf 100
   router-id 10.255.0.1
```

5. На физических интерфейсах включить OSPF:

```text
interface Ethernet1
   no switchport
   ip address 10.0.1.0/31
   ip ospf area 0
   ip ospf network point-to-point
```

---

## 2. Адресное пространство

| Назначение | Сеть |
|---|---|
| SPINE1 P2P | `10.0.1.0/24` |
| SPINE2 P2P | `10.0.2.0/24` |
| Loopback0 | `10.255.0.0/24` |
| Loopback1 / VTEP | `10.255.1.0/24` |

---

## 3. Loopback0

| Device | IP Address |
|---|---|
| SPINE1 | `10.255.0.1/32` |
| SPINE2 | `10.255.0.2/32` |
| LEAF1 | `10.255.0.11/32` |
| LEAF2 | `10.255.0.12/32` |
| LEAF3 | `10.255.0.13/32` |

---

## 4. Полезный Markdown-синтаксис

### Заголовки

```markdown
# Заголовок первого уровня
## Заголовок второго уровня
### Заголовок третьего уровня
#### Заголовок четвертого уровня
```

### Жирный текст

```markdown
**жирный текст**
```

Результат:

**жирный текст**

### Курсив

```markdown
*курсив*
```

### Команды и IP-адреса в тексте

Используется один обратный апостроф:

```markdown
`Loopback0`
`10.255.0.1/32`
`ip ospf area 0`
```

Результат:

`Loopback0`

`10.255.0.1/32`

`ip ospf area 0`

### Блок конфигурации

Используются три обратных апострофа:

````markdown
```text
router ospf 100
   router-id 10.255.0.1
```
